import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.crm.app',
  appName: 'hr-crm',
  webDir: 'dist/hr-crm-webapp/browser'
};

export default config;
